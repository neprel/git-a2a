# git-a2a.com static package

This directory is the local-first static documentation package for schema 2. Validate it with
`make site-check`. Publication is a separate, explicitly authorized operation; do not deploy as
part of ordinary documentation work.
