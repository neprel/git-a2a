# Explicit source materialisation when no native Git integration applies.
# yaml-language-server: $schema=https://git-a2a.com/schema/a2amodule.v2.json
schema: 2
component:
  id: firmware-consumer
dependencies:
  - name: board-support
    git: ssh://git@example.com/embedded/board-support.git
    ref: v4
    bindings:
      - adapter: submodule
        variant: git
        path: deps/board-support
